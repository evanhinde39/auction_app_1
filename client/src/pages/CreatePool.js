import React from "react";

const CreatePool = () => {
  return (
    <div>
      <h2>Create Pool</h2>
      <p>This is the Create Pool page where users can create new pools.</p>
    </div>
  );
};

export default CreatePool;
